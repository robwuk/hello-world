import React from "react"
import Header from "../components/header"

export default () => (
  <div style={{ color: `teal` }}>
    <Header headerText="Monkton Park Church" />
    <Header headerText="Does this put another header in?" />
    <p>Such wow. Very React.</p>
  </div>
)
